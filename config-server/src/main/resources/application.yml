spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Esr0g/my-tiny-library.git
          search-paths: config-server/configs

server:
  port: 8888
